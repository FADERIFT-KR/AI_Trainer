# Two-stage squat assessment

Run `python scripts/train_two_stage_squat.py` against the configured air-squat dataset. The script writes `output/two_stage_squat/normal_template.npz`, `mt_stgcn.pt`, and `report.json`. The game loads these files automatically at start.

1. Refined 3D labels are mapped to the 18-joint common skeleton. Every frame is pelvis-centered; the median bilateral hip-knee-ankle length of a repetition is set to one. The existing reference and live pipelines also align the body's axes using the initial standing frames. This preserves movement-related tilt while removing starting camera orientation.
2. Correct repetitions from fitting actors are time-resampled to 64 frames. One of up to eight sampled tracks initializes a DTW barycenter. All correct tracks are aligned to it, averaged, and realigned for three iterations. This 64-frame average is the normal template.
3. A disjoint group of *training* actors calibrates a distance threshold by maximizing binary F1. For each completed user repetition, DTW supplies both mean path distance and an aligned 64-frame skeleton. Its pose cost emphasizes legs, feet and trunk; arm placement has zero weight. A distance at or below the threshold passes as normal. Otherwise the aligned skeleton goes to the graph network.
4. The network has a shared stack of three bone-adjacency spatial graph blocks with temporal convolutions. An error head predicts one of the three dataset error classes; a multilabel head predicts ankle/foot, knee, hip, and trunk involvement. Training applies a small monotonic time-warp augmentation. The anatomical head uses **weak labels derived from the error class**, since the dataset does not contain independent body-part annotations. Its probabilities are hypotheses, not validated causal localization.
5. Actors in `configs/actor_split.json` marked `val` are excluded from template fitting, threshold calibration, and checkpoint selection. The JSON report records normal/error gate metrics, combined four-class accuracy, confusion counts, and actor IDs. During development, a first validation run motivated the lower-body distance weighting, so the same actor split is **exploratory validation**, not a sealed final test. Webcam MediaPipe world landmarks may differ in scale, shape, visibility, and geometry; a fresh actor holdout and labeled real-camera test are needed before treating the threshold as calibrated for deployment.

If the template exists but the graph checkpoint is missing, failed gate results remain `판정 불확실`. If both new artifacts are absent, the earlier DL/DTW path remains available for older installations.

For side-view scoring, see [visible-joint side scoring](side_view_visible_joint_scoring.md). Its separately trained artifacts are required; a missing side model never falls back to the unmasked frontal classifier.

The cited [Time-warping Augmentation and ST-GCN for Physical Rehabilitation Exercise Assessment](https://doi.org/10.1109/ICoICT66265.2025.11192964) motivates temporal augmentation and graph modeling. The DTW acceptance gate and multi-task anatomical head here are project-specific extensions; they are not claimed as results of that paper. The basic ST-GCN graph formulation follows [Yan et al.](https://arxiv.org/abs/1801.07455).
